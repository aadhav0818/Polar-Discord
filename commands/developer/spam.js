const Discord = require('discord.js');
const config = require('../../config.json');

module.exports = {
    name: 'code_big_blubber',
    async execute(client, message, args, Discord) { 
        message.channel.send('')
    }
}